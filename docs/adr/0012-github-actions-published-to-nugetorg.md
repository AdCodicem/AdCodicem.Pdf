# 12. GitHub Actions, published to nuget.org

Date: 2026-09-12

## Status

Accepted

## Context

The repository is on GitHub, and its CI can be inspected
from a development session, which is not true of Azure Pipelines. Publication uses trusted publishing, not
an API key — see D24.

## Decision

We will the repository is on GitHub, and its CI can be inspected
from a development session, which is not true of Azure Pipelines. Publication uses trusted publishing, not
an API key — see D24.

## Consequences

Recorded so the choice is not silently revisited.

---

Recorded as `D11` before this project adopted Architecture Decision Records; the identifier still
appears in commit messages and in `CLAUDE.md`.
