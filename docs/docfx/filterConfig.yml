# What DocFX leaves out of the generated API reference.
#
# An analyser suppression is a note from the code to its reviewers, not part of the contract a caller
# depends on; printed in a declaration it reads as though it were.
attributeRules:
  - exclude:
      uidRegex: ^System\.Diagnostics\.CodeAnalysis\.SuppressMessageAttribute$
      type: Type
